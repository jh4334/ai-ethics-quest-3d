# 사라진 학생 H-17 — 2D 디자인 계약

## 목표 경험

초등 5–6학년 플레이어가 복잡한 이동·전투 없이 한 장의 밤 캠퍼스 원화를 살펴보며, 사라진 친구 하루의 기록을 어떻게 다룰지 선택하는 짧은 2D 이야기 게임이다.

## 기준 화면

- 유일한 시각 기준은 `docs/design/concepts/gameplay-screen-v3.webp`다.
- 사용자가 요청한 대로 이 원화를 런타임의 실제 2D 장면으로 사용한다. 잘라 붙인 3D처럼 주장하지 않는다.
- 데스크톱에서는 원화 전체를 가로 중앙에 `contain`으로 표시하고 남는 영역은 짙은 남색으로 둔다.
- 세로형 모바일에서도 원화 전체를 자르지 않는다. 화면 아래 여백에는 큰 `행동` 버튼과 도움말 버튼만 둔다.
- 시작 화면에는 새 패널을 덧씌우지 않는다. 원화에 이미 그려진 임무·진행도·상태·상호작용 UI를 그대로 보존한다.

## 상호작용

- 데스크톱: 원화 속 `E 살펴보기` 영역 클릭 또는 `E` 키.
- 모바일: 화면 오른쪽 아래 `행동` 버튼.
- 네 번의 짧은 대화 뒤 두 선택 중 하나를 누른다.
- 이동, 카메라 회전, 공격, 대시, 조합키는 사용하지 않는다.
- 이전 3D 버전은 도움말 대화상자의 링크로만 남겨 롤백 경로를 보존한다.

## 이야기 원칙

- 기존 핵심 인물인 하루, 도트, 플레이어를 유지한다.
- 잘린 대화가 퍼져 하루가 사라진 사건을 원본 맥락과 동의의 문제로 풀어낸다.
- 정답 점수를 표시하지 않고 선택 직후 하루 또는 도트의 반응으로 결과를 보여 준다.
- 잘못 퍼뜨리는 선택을 해도 비난하거나 게임오버시키지 않고 다시 확인할 기회를 준다.

## 접근성·성능

- 선택 버튼은 최소 48px, 모바일 주요 버튼은 최소 52px이다.
- 키보드 포커스는 호박색 윤곽선으로 표시한다.
- 한국어는 자연스러운 문장 단위로 쓰고 좁은 카드에서도 한 글자 고아 줄을 만들지 않는다.
- 장식 애니메이션은 사용하지 않는다.
- 초기 화면은 89KB 내외의 단일 WebP 원화와 소형 CSS/JS만 지연 없이 불러온다.
- WebGL이 필요 없으므로 WebGL 미지원 기기에서도 같은 화면과 선택 흐름이 동작한다.

## 허용된 차이

- 원화 자체의 가로 비율 때문에 세로형 모바일에는 상하 여백이 생긴다. 원화를 확대해 자르는 것보다 전체 구도를 보존하는 것을 우선한다.
- 이야기가 시작된 뒤에만 읽기 쉬운 대화 카드와 선택 카드를 원화 위에 표시한다.
