# H-17: NULL — 삭제된 밤: 성능 예산

이 문서는 대표 전투와 장시간 안정성의 이진 합격 기준을 고정한다. 측정값이 예산을 넘으면 콘텐츠를 추가하지 않고 원인을 줄인다.

## 측정 조건

- 데스크톱 기준 화면: `1180×820`, 정상 품질, DPR 상한 적용.
- 모바일 기준 화면: `390×844`, 저품질, 터치 UI, DPR 상한 적용.
- 대표 혼합 아레나는 근접 지우개, 원거리 도장기, TRACE 대상, 동시 피격·파티클 상한을 포함한다.
- 대표 보스는 출석 감독관 3페이즈, REFLECT 빔, 분신 TRACE, 약점 파괴를 포함한다.
- 각 장면은 워밍업 뒤 동일한 시드와 입력 로그로 측정한다. 평균값으로 p95 초과를 숨기지 않는다.
- 브라우저·운영체제·커밋·품질·화면·DPR·측정 시간을 결과 JSON에 기록한다.

## 공통 계약 레지스트리

### 정본 식별자

- 제목: `H-17: NULL — 삭제된 밤`
- 장: `00:17 — 출석번호 없음` / `웃는 얼굴의 폭동` / `두 개의 학교` / `3초 승인실` / `마지막 방송`
- 플레이 동사: `SIGNAL BLADE` / `DASH` / `REFLECT` / `TRACE`
- 결과 축: `Integrity` / `Exposure` / `Trust`

### 콘텐츠 안전 한계

- CS-01: 대상 연령은 12세 이상이다.
- CS-02: 학교·인물·사건은 허구이며 실제 개인이나 기관을 암시하지 않는다.
- CS-03: 이름·소속·이메일·연락처 등 실제 개인정보를 저장하거나 문서화하지 않는다.
- CS-04: 사적 대화·사진의 내용은 재현하지 않고 수집·삭제 사실만 다룬다.
- CS-05: 성적 콘텐츠와 성적 대상화를 넣지 않는다.
- CS-06: 유혈·신체 훼손·죽음의 직접 묘사를 넣지 않는다.
- CS-07: 자해·자살·모방 가능한 위험 행동을 소재나 해결책으로 쓰지 않는다.
- CS-08: 약물·도박·현금 결제·확률형 보상을 넣지 않는다.
- CS-09: 괴롭힘과 혐오는 추상화하며 비하어를 재현하거나 플레이어가 반복하게 하지 않는다.
- CS-10: 딥페이크 피해는 굴욕적 영상 없이 출처와 확산 흔적으로 표현한다.
- CS-11: 피해자 비난, 정답/오답 낙인, 단일 도덕 점수를 사용하지 않는다.
- CS-12: AI나 한 사람을 순수 악으로 만들지 않고 연결된 책임 경로를 보여 준다.
- CS-13: 필수 진행을 위해 불필요한 개인정보 공개를 요구하지 않는다.
- CS-14: 초당 3회를 넘는 섬광을 금지하고 reduced motion에서 흔들림·섬광을 대체한다.
- CS-15: 갑작스러운 큰 소리에 의존하지 않으며 핵심 오디오에는 자막·시각 단서를 제공한다.
- CS-16: 일시정지·음소거·자막·재시도를 언제든 사용할 수 있고 공포 연출로 이를 막지 않는다.

### 성능 예산

- 대표 전투 p95 프레임: 데스크톱 `16.7ms 이하`, 모바일 `33.3ms 이하`.
- DPR 상한: 데스크톱 `2.0`, 모바일 `1.75`.
- 대표 아레나: draw calls `250 이하`, triangles `150000 이하`.
- 조명: 동적 그림자 광원 `2 이하`, 전체 동적 광원 `4 이하`.
- 파티클: 동시 `250 이하`; 프레임 루프 신규 객체 할당 `0`.
- 안정성: 20분 반복 조우 뒤 JS heap 증가율 `10% 이하`; 콘솔 오류 `0`.

## 합격 판정표

| 지표 | 데스크톱 | 모바일 | 실패 처리 |
| --- | ---: | ---: | --- |
| 대표 전투 p95 프레임 | ≤ 16.7ms | ≤ 33.3ms | 장식 효과, 투명 재질, 오버드로 순으로 줄이고 재측정 |
| DPR | ≤ 2.0 | ≤ 1.75 | 렌더러 설정 오류로 즉시 실패 |
| draw calls | ≤ 250 | ≤ 250 | 재질·메시 배치와 풀링 점검 |
| triangles | ≤ 150000 | ≤ 150000 | 화면 기여가 낮은 장식 LOD 축소 |
| 동적 그림자 광원 | ≤ 2 | ≤ 2 | 그림자 없는 발광·베이크 대안 사용 |
| 전체 동적 광원 | ≤ 4 | ≤ 4 | 상시 조명을 제거하고 재측정 |
| 동시 파티클 | ≤ 250 | ≤ 250 | 비핵심 효과부터 캡 적용 |
| 프레임 루프 신규 객체 할당 | 0 | 0 | 사전 할당·풀링 뒤 재측정 |
| 20분 뒤 JS heap 증가율 | ≤ 10% | ≤ 10% | 씬·리스너·풀 반환 누수 추적 |
| 콘솔 오류 | 0 | 0 | 원인 해결 전 출시 차단 |

## 품질 저하 우선순위

가독성을 유지하면서 후처리, 장식 파티클, 반사 해상도, 장식 메시 밀도 순으로 낮춘다. 적 예고, 피격 확인, TRACE 실루엣, REFLECT 방향, 자막, 터치 입력 표시는 어떤 품질에서도 제거하지 않는다. 모바일 저품질에서는 후처리를 끄되 시뮬레이션 틱을 낮추지 않는다.

## 장시간·상태 전환 검증

20분 반복 조우 동안 일시정지, 재시작, 음소거, reduced motion, 화면 회전, 숨겨진 탭 복귀를 포함한다. 각 전환 뒤 리소스와 리스너 수가 기준으로 돌아와야 한다. 측정 명령이 성공 코드를 냈더라도 필수 지표가 빠진 결과는 실패로 판정한다.
