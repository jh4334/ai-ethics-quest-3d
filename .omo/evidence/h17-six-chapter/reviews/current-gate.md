# H-17 현재 게이트 판정

- 제품 코드 SHA: `1a1309845f18`
- 판정: **사용자 검토 가능, 레퍼런스 충실도 미승인**
- 범위: 1~6장 실행 구조, 저장 마이그레이션, 키보드·터치 완주, 최신 12화면, 성능·다운로드 보고.

## 통과한 항목

- Playwright E2E 23/23, 키보드·터치 1~6장 완주, 최종 `chapter-6:resolved-redacted` 저장.
- lazy 환경 에셋 캐시 쓰기를 응답 수명주기에 포함하고, 첫 로드가 워커 제어보다 빠른 경우 로더가 현재 장 URL을 보류했다가 제어권 획득 뒤 전달한다. 동일 출처 환경 경로만 허용하는 두 계약과 결과 패널 중복 DOM 쓰기 방지 계약을 포함해 `npm test` 507/507, H-17 307/307, legacy 200/200과 CI 모드 오프라인 Playwright 3/3이 통과했다.
- 두 입력 경로 모두 콘솔 오류 0건, 4xx/5xx 응답 0건; 터치 조작 8개 모두 최소 44px.
- 최신 P0 하드웨어 WebGL 5화면에서 최대 197 draw calls, 152,654 triangles, 4 lights.
- Intel Iris Xe D3D11 직접 rAF p95: 데스크톱 8.5ms 이하, 태블릿·모바일 16.7ms 이하.
- 건축 재질 역할명을 실제 Three.js 재질에 기록해 벽돌·콘크리트·목재·트랙의 ambientCG PBR 교체가 렌더에서 실행된다. 첫 상판은 비정형 포장과 높이차 암석 림을 사용한다.
- v4 원본 바이트 백업, `legacy-grandfathered` 감사 결과, `verified-package` 식별자 통일은 이전 아키텍처 리뷰 블로커를 해소했다.

## 미통과 항목

- `reference-diff.json`의 동일 1440×900 기계 비교는 similarity 0/100이다. 레퍼런스와 구현이 동일 장면 복제가 아니므로 점수 자체가 품질 척도는 아니지만, 재질 밀도·식생·실루엣·조명·캐릭터 의상에서 큰 시각 격차가 실제로 남아 있다.
- 현재 구현은 실제 GLB·PBR 재질·다중 공간을 쓰는 스타일라이즈드 로우폴리 게임이다. 첨부·저장소 콘셉트의 AAA급 시각 품질을 달성했다고 판정할 수 없다.
- 이전 독립 시각 리뷰는 이 격차를 블로커로 판정했다. 사용자 제한인 총 3개 서브에이전트를 모두 사용해 제품 SHA `1a1309845f18`에 대한 새 독립 승인 라운드는 실행하지 않았다. 자가 점검을 독립 PASS로 대체하지 않는다.

## 권고

- 기능·캠페인·저장·제품 UI·성능 검토용 PR은 열 수 있다.
- main 병합 전 사용자가 최신 비교 시트를 직접 확인하고, 현재 로우폴리 아트 방향을 수용할지 추가 환경·캐릭터 아트 패스를 요구할지 결정해야 한다.
